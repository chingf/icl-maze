env: tree
n_envs: 1000
horizon: 1600
max_layers: 7
branching_prob: 1.
node_encoding: random  #  If null, state_dim = 2. If random, state_dim = 10. If onehot, state_dim = 2^max_layers - 1
state_dim: 10
action_dim: 4
store_gpu: True
rollin_type: explore

# For data creation and collection purposes
goal_total_seeds: 0
n_search_seeds: 0